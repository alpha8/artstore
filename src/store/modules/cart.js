/* jshint esnext: true */
import * as types from '../types';
import { save, load } from '@/common/js/store';
import api from '@/api/api';

// initial state
const state = {
};

// getters
const getters = {
};

// actions
const actions = {
};

// mutations
const mutations = {
};

export default {
  state,
  getters,
  actions,
  mutations
};
